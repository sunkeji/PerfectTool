<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.skj.wheel">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".album.activity.AlbumAllActivity" />
        <activity android:name=".album.activity.AlbumFolderActivity" />
        <activity android:name=".album.activity.AlbumFloderImageActivity" />
        <activity android:name=".album.activity.AlbumVPFixedActivity" />
    </application>

</manifest>
